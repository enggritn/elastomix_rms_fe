@{
    ViewBag.Title = "Formula";
    ViewBag.Create = String.Concat(ViewBag.Title.Replace(" ", ""), "/Create");
}

<div class="page-header">
    <div class="row align-items-end">
        <div class="col-lg-8">
            <div class="page-header-title">
                <i class="ik ik-inbox bg-blue"></i>
                <div class="d-inline">
                    <h5>Create @ViewBag.Title</h5>
                    <span>List</span>
                </div>
            </div>
        </div>
        <div class="col-lg-4">
            <nav class="breadcrumb-container" aria-label="breadcrumb">
                <ol class="breadcrumb">
                    <li class="breadcrumb-item">
                        <a href=""><i class="ik ik-database"></i></a>
                    </li>
                    <li class="breadcrumb-item">
                        <a href="#">Master</a>
                    </li>
                    <li class="breadcrumb-item active" aria-current="page">Create @ViewBag.Title</li>
                </ol>
            </nav>
        </div>
    </div>
</div>

<div class="row">
    <div class="col-md-12">
        <div class="card">
            <div class="card-body">
                <div class="row">
                    <div class="col-lg-12">
                        <div class="form-group row">
                            <label class="col-form-label col-sm-4">Recipe Code</label>
                            <div class="col-sm-8">
                                @Html.TextBox("RecipeCode", null, new { @class = "form-control" })
                                <span class="help-block text-danger"></span>
                            </div>
                        </div>
                        <div class="form-group row">
                            <label class="col-form-label col-sm-4">Product Name</label>
                            <div class="col-sm-8">
                                @Html.TextBox("ProductName", null, new { @class = "form-control" })
                                <span class="help-block text-danger"></span>
                            </div>
                        </div>
                        <div class="form-group row">
                            <label class="col-form-label col-sm-4">Date Issued</label>
                            <div class="col-sm-8">
                                @Html.TextBox("DateIssued", "hh / bb / tttt", new { @class = "form-control" })
                                <span class="help-block text-danger"></span>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
            <div class="card-footer">
                <a href="@Url.Action("Index", "Formula")" class="btn btn-outline-info float-left"><i class="ik ik-arrow-left"></i>Back</a>
                <button id="btnSave" type="button" class="btn btn-primary float-right"><i class="ik ik-save"></i>Save</button>
            </div>
        </div>
    </div>
</div>

<!-- Modal -->
<div class="modal" id="saveModal" tabindex="-1" role="dialog" aria-hidden="true">
    <div class="modal-dialog" role="document">
        <div class="modal-content">
            <div class="modal-header">
                <h5 class="modal-title" id="confirmationModalTitle">Save Data</h5>
                <button type="button" class="close" data-dismiss="modal" aria-label="Close">
                    <span aria-hidden="true">&times;</span>
                </button>
            </div>
            <div class="modal-body">
                <p>Are you sure ?</p>
            </div>
            <div class="modal-footer">
                <button type="button" class="btn btn-danger" data-dismiss="modal"><i class="ik ik-x-circle"></i>No</button>
                <button type="button" class="btn btn-success" id="btnYes"><i class="ik ik-check-circle"></i>Yes</button>
            </div>
        </div>
    </div>
</div>

@section custom_js
{
    <script type="text/javascript">
        @*let server = '@ViewBag.server';
        var isValid = true;
        $("#btnSave").click(function() {
            clearForm();
            if ($('#Username').val().length == 0) {
                $('#Username').addClass('is-invalid');
                $('#UsernameErrorMsg').html('This field is Required');
                isValid = false;
            }
            if ($('#UserEmail').val().length == 0) {
                $('#UserEmail').addClass('is-invalid');
                $('#UserEmailErrorMsg').html('This field is Required');
                isValid = false;
            }
            if ($('#Password').val().length == 0) {
                $('#Password').addClass('is-invalid');
                $('#PasswordErrorMsg').html('This field is Required');
                isValid = false;
            }

            if (isValid) {
                $('#saveModal').modal('show');
            }
        });

        function clearForm() {
            $('#Username').removeClass('is-invalid');
            $('#UsernameErrorMsg').html('');
            $('#UserEmail').removeClass('is-invalid');
            $('#UserEmailErrorMsg').html('');
        }

        function showFlash() {
            toastr.success('Data Successfully Saved')
        }

        function dissmissDialog() {
            $('#saveModal').modal('hide');
        }

        $('#btnYes').click(function () {
            $.ajax({
                    type: 'POST',
                    dataType: "json",
                    beforeSend: function (request) {
                        request.setRequestHeader('Token', 'Naufal');
                    },
                    url: server + 'Api/@ViewBag.Title/Create',
                    data: {
                        Username: $('#Username').val(),
                        Password: $('#Password').val(),
		                UserEmail: $('#UserEmail').val()
                    },
                    success: function (response) {
                        isValid = response.status;
                        if (!isValid) {
                            $.each(response.error_validation, function (index, value) {
                                $('#' + + value.FieldName + 'ErrorMsg').html(value.ErrorMessage);
                                $('#' + value.FieldName).addClass('is-invalid');
                            });
                            dissmissDialog();
                        } else {
                            clearForm();
                            dissmissDialog();
                            showFlash();
                        }
                    }
                });
        });*@
    </script>
}
